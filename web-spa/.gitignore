# dependencies
node_modules/

# build output
dist/

# local env (never commit real hosts/secrets)
.env
.env.local
.env.*.local

# logs & editor cruft
*.log
.DS_Store

# generated API types (produced by `npm run gen:api` against the live seam)
src/api/schema.d.ts

# vite / tsc caches
.vite/
*.tsbuildinfo
