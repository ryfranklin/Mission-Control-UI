# dependencies
node_modules/

# build output
dist/

# local env (never commit real hosts/secrets)
.env
.env.local
.env.*.local

# logs & editor cruft
*.log
.DS_Store

# Generated API types (`npm run gen:api`) are COMMITTED at src/api/schema.ts,
# so the typed client builds without a live seam. Regenerate against the live
# seam when the OpenAPI contract changes; do not hand-edit schema.ts.

# vite / tsc caches
.vite/
*.tsbuildinfo
